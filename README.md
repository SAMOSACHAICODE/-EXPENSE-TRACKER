# -EXPENSE-TRACKER
This Python CLI script tracks expenses using a local CSV file. Users can add, view, delete, and filter transactions via a menu. It features automatic ID generation, date defaults, category filtering, and monthly summaries, utilizing standard libraries for lightweight, persistent data management..
